<ul class="work-grid">
    <slot />
</ul>

<style>
    .work-grid {
        display: grid;
        grid-template-columns: repeat(
            auto-fill,
            minmax(min(100%, 18rem), 1fr)
        );
        gap: 1.5rem;
        margin: 0;
        padding: 0;
        list-style: none;
    }
</style>